#include "main.h"
/**
 *add - returns the addition of two numbers
 *@a: one int to be added
 *@b: one int to be added
 *Return: returns the value of a + b
 */

int add(int a, int b)
{
	int c = a + b;

	return (c);
}
