#include <stdio.h>
/**
 * mul - returns the value of two ints multiplied together
 * @a: one int to be multipled
 * @b: second int to be multipled
 *Return: the total of a * b
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
